<?xml version="1.0" encoding="UTF-8" ?>
<UserControl x:Class="NTC.FamilyManager.Views.FamilyExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1000"
             Background="#F3F4F6">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MahApps.Metro.ALB;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="/MahApps.Metro.ALB;component/styles/light.blue.xaml" />
                <ResourceDictionary Source="/MaterialDesignThemes.Wpf.ALB;component/Themes/materialdesigntheme.light.xaml" />
                <ResourceDictionary Source="/MaterialDesignThemes.Wpf.ALB;component/Themes/materialdesigntheme.defaults.xaml" />
                <ResourceDictionary Source="/AlphaBIMWPF;component/Resource/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0"
                Background="White"
                BorderBrush="#E5E7EB"
                BorderThickness="0,0,1,0">
            <StackPanel Margin="15">
                <TextBlock Text="PHÂN LOẠI"
                           FontWeight="Bold"
                           FontSize="12"
                           Foreground="#6B7280"
                           Margin="0,0,0,15" />
                <Expander Header="Phiên bản Revit"
                          IsExpanded="True"
                          Margin="0,0,0,10"
                          Style="{DynamicResource MaterialDesignExpander}">
                    <StackPanel Margin="10,5,0,0">
                        <CheckBox Content="Revit 2021"
                                  IsChecked="True"
                                  Foreground="#374151"
                                  Margin="0,2" />
                        <CheckBox Content="Revit 2022"
                                  Foreground="#374151"
                                  Margin="0,2" />
                        <CheckBox Content="Revit 2023"
                                  Foreground="#374151"
                                  Margin="0,2" />
                    </StackPanel>
                </Expander>
                <Expander Header="Hạng mục"
                          IsExpanded="True"
                          Style="{DynamicResource MaterialDesignExpander}">
                    <StackPanel Margin="10,5,0,0">
                        <CheckBox Content="Architecture"
                                  IsChecked="True"
                                  Foreground="#374151"
                                  Margin="0,2" />
                        <CheckBox Content="Structure"
                                  Foreground="#374151"
                                  Margin="0,2" />
                    </StackPanel>
                </Expander>
            </StackPanel>
        </Border>
        <Grid Grid.Column="1"
              Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0"
                    Background="White"
                    Margin="0,0,0,20"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="200" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Tìm Family..."
                             Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,10,0"
                             Padding="10" />
                    <ComboBox Grid.Column="1"
                              Style="{DynamicResource MaterialDesignOutlinedComboBox}"
                              SelectedIndex="0">
                        <ComboBoxItem Content="Từ A đến Z" />
                        <ComboBoxItem Content="Mới nhất" />
                    </ComboBox>
                </Grid>
            </Border>
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Families}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Width="180"
                                                 Margin="0,0,15,15"
                                                 Background="White"
                                                 Padding="10">
                                <StackPanel>
                                    <Grid Height="120"
                                          Background="#F9FAFB">
                                        <Image Source="{Binding ThumbnailUrl}"
                                               Stretch="Uniform"
                                               Margin="5" />
                                        <Button VerticalAlignment="Bottom"
                                                HorizontalAlignment="Right"
                                                Margin="5"
                                                Width="30"
                                                Height="30"
                                                Command="{Binding DataContext.LoadToRevitCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{DynamicResource MaterialDesignFloatingActionMiniSecondaryButton}">
                                            <materialDesign:PackIcon Kind="Download"
                                                                     Width="20"
                                                                     Height="20" />
                                        </Button>
                                    </Grid>
                                    <StackPanel Margin="0,10,0,0">
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="Bold"
                                                   Foreground="#111827"
                                                   TextTrimming="CharacterEllipsis" />
                                        <StackPanel Orientation="Horizontal"
                                                    Margin="0,5,0,0">
                                            <materialDesign:PackIcon Kind="Heart"
                                                                     Width="14"
                                                                     Foreground="#9CA3AF" />
                                            <TextBlock Text="{Binding Likes}"
                                                       Foreground="#9CA3AF"
                                                       FontSize="11"
                                                       Margin="4,0,10,0" />
                                            <materialDesign:PackIcon Kind="Download"
                                                                     Width="14"
                                                                     Foreground="#9CA3AF" />
                                            <TextBlock Text="{Binding Downloads}"
                                                       Foreground="#9CA3AF"
                                                       FontSize="11"
                                                       Margin="4,0,0,0" />
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,10,0,0"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True"
                             Width="20"
                             Height="20"
                             Margin="0,0,10,0" />
                <TextBlock Text="Đang xử lý..."
                           VerticalAlignment="Center"
                           Foreground="#6B7280" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>